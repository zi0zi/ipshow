<Window x:Class="IpShow.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="IpShow"
        Width="340" Height="90"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
  <Window.ContextMenu>
    <ContextMenu>
      <MenuItem Header="Dock Top" IsCheckable="True" Name="DockTopMenu" Click="DockTopMenu_Click" />
      <MenuItem Header="Dock Bottom" IsCheckable="True" Name="DockBottomMenu" Click="DockBottomMenu_Click" />
      <Separator />
      <MenuItem Header="Free Move" IsCheckable="True" Name="DockFreeMenu" Click="DockFreeMenu_Click" />
      <Separator />
      <MenuItem Header="Refresh Now" Click="RefreshNowMenu_Click" />
      <MenuItem Header="Exit" Click="ExitMenu_Click" />
    </ContextMenu>
  </Window.ContextMenu>

  <Grid>
    <Border CornerRadius="14" Padding="14" Effect="{StaticResource BannerShadow}">
      <Border.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
          <GradientStop Color="#F0212121" Offset="0" />
          <GradientStop Color="#E82A2A2A" Offset="0.6" />
          <GradientStop Color="#CC1A2B3B" Offset="1" />
        </LinearGradientBrush>
      </Border.Background>
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding IpAddress}"
                   Foreground="{StaticResource TextPrimary}"
                   FontFamily="Bahnschrift"
                   FontSize="26"
                   FontWeight="SemiBold"
                   TextTrimming="CharacterEllipsis" />
        <TextBlock Grid.Row="1"
                   Text="{Binding LocationText}"
                   Foreground="{StaticResource TextSecondary}"
                   FontFamily="Bahnschrift"
                   FontSize="12"
                   Margin="0,2,0,0"
                   TextTrimming="CharacterEllipsis" />
        <TextBlock Grid.Row="2"
                   Text="{Binding StatusText}"
                   Foreground="{Binding StatusBrush}"
                   FontFamily="Bahnschrift"
                   FontSize="10"
                   Margin="0,2,0,0"
                   TextTrimming="CharacterEllipsis" />
      </Grid>
    </Border>
  </Grid>
</Window>
